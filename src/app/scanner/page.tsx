import { ScannerPage } from "@/components/scanner-page";

export default function Page() {
  return (
    <ScannerPage
      title="Sector Scanner"
      subtitle="Deep drawdown → long-term weakness → current trend → relative strength → recovery evidence. Not a buy list."
    />
  );
}
